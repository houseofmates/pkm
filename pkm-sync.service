[Unit]
Description=PKM Auto-Sync to GitHub
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/pkm
ExecStart=%h/pkm/autosync.sh
Restart=always
RestartSec=10

[Install]
WantedBy=default.target
