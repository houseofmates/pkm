# Fix displayname update bug and improve table header styling

## Tasks
- [x] Add `onFieldUpdated={fetchData}` prop to `CurrentViewComponent` in `collection-detail.tsx`
- [x] Add `onFieldUpdated` to `ViewProps` type in `registry.tsx`
- [ ] Center property titles/names in table view headers
- [ ] Increase font size of property titles in table view headers (make them slightly less small)
