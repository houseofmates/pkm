# nested tags

nested tags let you create hierarchical tag structures like `project/pkm/frontend` or `mood/calm`.

## how they work

- use `/` as a separator to create depth
- parent tags automatically include children in searches
- combine with [[selects]] or [[tag-databases]] for richer taxonomy

nested tags bridge the gap between flat [[tagging]] and rigid folder structures. see also: [[collections]], [[fields]]
