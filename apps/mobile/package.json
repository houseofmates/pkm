{
  "name": "pkm-mobile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=pkm-web",
    "build": "npm run build --workspace=pkm-web && npx cap sync android"
  },
  "dependencies": {
    "@pkm/core": "*",
    "framer-motion": "^12.25.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "zustand": "^5.0.9"
  }
}